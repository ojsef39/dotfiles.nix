# Setup & Deployment

This configuration is managed using `nh` (Nix Helper) and `just`.

<!-- Four-backtick fence: the justfile itself contains a ``` block (just's
     multi-line backtick syntax for `nix_flags`), which would otherwise close
     a normal three-backtick fence early and spill the rest as prose. -->

````just
{{#include ../../justfile}}
````
